/**
* Empty object which namespaces all of our components and custom javascript, in order to avoid polluting the global object.
*/
var projectName = {};

/**
* Run everything on document ready, and pass in $ so it can safely be used without conflicts.
*/
jQuery(function($) {
'use strict';

/**
* App code is automatically placed here
*/